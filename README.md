I created a CI/CD pipeline for a Node.js to-do application using Jenkins and GitHub Actions. 
I started by setting up an AWS instance and installing Jenkins, then integrated it with GitHub by generating SSH keys.
Then manually installed all the required dependencies and run the application.

